# Codex Desk Pet v1.1.1

这次继续优化桌面占用和回复阅读体验。

## 更小的桌宠

- 常驻窗口由 `284×382` 缩短为 `284×344`
- 角色本体由约 270 像素宽缩小到约 226 像素宽
- 提问卡仍然只在需要时临时展开，发送后自动缩回

## 更大的回复气泡

- 回复气泡最大宽度由 260 像素增加到 272 像素
- 最大高度由 76 像素增加到 112 像素，可显示更多回答内容
- 长回答可以在气泡内滚动，完成回复的停留时间由 9 秒延长到 12 秒

## 安全说明

快速提问使用 Codex 的只读沙箱和 `never` 审批策略，不会通过桌宠修改本地文件或执行需要授权的操作。桌宠不内置 API Key，也不保存账号凭据。

> 当前安装包未进行 Apple Developer 签名和公证。首次启动如被 macOS 拦截，请在 Finder 中右键应用并选择“打开”。
