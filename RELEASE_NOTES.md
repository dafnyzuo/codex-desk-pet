# Codex Desk Pet v1.2.1

这次重点解决“动作看不出区别”和“靠边没有吸附”两个体验问题。

## 7 个真正不同的动作

- 为眨眼、点头、跳跃、探头、挥手、伸懒腰和受惊摇晃分别加入独立透明姿势帧
- 不再只让同一张原图做轻微位移，每个动作都会切换人物姿势和对应提示语
- 右键桌宠打开“动作预览”，可以逐个检查全部动作
- 空闲时随机播放动作，并避免连续重复同一个

## 更可靠的屏幕边缘吸附

- 拖动到边缘附近时实时磁吸，不必等到松手才看到效果
- 吸附时显示左、右、上、下方向提示
- 根据当前 Dock 位置和图标尺寸预留安全区域，同时避开菜单栏
- 启动时也会把旧位置自动修正到安全区域内

## 窗口比例

- 默认保持更小的 `284×344` 桌宠窗口
- 回复气泡加宽、加高，长回答可滚动查看
- 仍可通过右键菜单切换迷你、小巧和标准三档尺寸

## 安全说明

快速提问使用 Codex 的只读沙箱和 `never` 审批策略，不会通过桌宠修改本地文件或执行需要授权的操作。桌宠不内置 API Key，也不保存账号凭据。

> 当前安装包未进行 Apple Developer 签名和公证。首次启动如被 macOS 拦截，请在 Finder 中右键应用并选择“打开”。
